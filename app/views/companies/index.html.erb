<div class="container text-center">
  <div class="row">
    <h1>Compañias</h1>
    <%= link_to "Crear", new_company_path(@company), class: 'btn btn-primary mb-5' %>
    <% @companies.each do |company| %>
      <div class="col-lg-8 mb-3">
        <%= company.name %>
        <%= link_to "Vehiculos registrados", company_path(company)   %>
        <%= link_to "Editar", edit_company_path(company) %>
        <%= link_to "Borrar", company_path(company), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
      </div>
    <% end %>
  </div>
</div>
