<div class="container mt-4">
  <%= simple_form_for @company do |f| %>
    <div class="mb-3">
      <%= f.input :name, label: 'Nombre de la compañía', error: '* campo obligatorio', label_html: {class: 'form-label'}, input_html: {class: 'form-control'} %>
    </div>
      <%= f.submit 'Aceptar', class: 'btn btn-primary mb-5' %>
  <% end %>
  <%= link_to "Back", companies_path %>
</div>
